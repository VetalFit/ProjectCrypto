import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => {
	return {
		iconBlock: {
			paddingTop: '10px',
			paddingRight: '10px',
		},
		themeIcon: {
			marginRight: '45px',
		},
	};
});
